import styled from 'styled-components';
import { Card, FlexBox, Input } from '../styled';

const DataManagementContainer = styled(Card)`
`;

const DataManagementButtonContainer = styled(FlexBox)`
`;

const HiddenFileInput = styled(Input)`
    display: none;
`;

export {
    DataManagementContainer,
    DataManagementButtonContainer,
    HiddenFileInput
};
